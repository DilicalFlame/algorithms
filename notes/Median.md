# Median

> Middle value separating the greater and lesser halves of a data set.

## Formulae

For a data set $x$ of $n$ elements, ordered from smallest to greatest i.e. **sorted**:
- if $n$ is odd, $$med(x) = x_{{n+1}/2}$$
- if $n$ is even, $$med(x) = \frac{{x_{n/2} + x_{(n/2)+1}}}{2}$$

